# Team Coordination

This directory contains coordination metadata for parallel team work. It does
not define product behavior or replace Harness story state.

- [`now/`](now/README.md) contains the active workstream trackers and the
  human-to-tracker identity map.

Read the `now/` index before selecting or implementing a product workstream.
