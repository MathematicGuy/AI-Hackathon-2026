# Execution Plan

1. Add failing tests for `AdvisorError`, display alternative, conditional recovery span, and unchanged role enum.
2. Run the targeted contract suite and record RED.
3. Add the minimal schema/constants and dependency manifest.
4. Update the product contract with the reconciliation rules.
5. Run the targeted contract suite and dependency-resolution dry run.
6. Run separate task review and close all Critical/Important findings.

Implementation details are Task 0 in `docs/superpowers/plans/2026-07-17-m1-1-through-m1-8.md`.
