# AI Coding Session Log

## Session Metadata

- `member`: Lưu Tiến Duy
- `member_slug`: luu-tien-duy
- `ai_client`: Claude Code
- `interface`: VS Code extension
- `session_id`: 13a09b954700
- `started_at_utc`: 2026-07-18T22:05:43Z
- `task_summary`: Audit the deploy branch for frontend and backend defects blocking production, then implement the fixes.
- `repository`: AI-Hackathon-2026
- `branch`: deploy

## Objective

Identify the frontend, backend, and deployment defects that block a production
release of the `deploy` branch, then implement the agreed fixes under a
registered high-risk story without regressing behavior that already works.

## Interaction Log

### Entry 1 — 2026-07-18T20:15:00Z

- Human request summary: Analyse the repository and list the frontend and
  backend defects that must be fixed to complete a production deploy.
- AI response or decision summary: Ran the mandatory logging and repository
  read gates, then audited the backend, frontend, and deployment surfaces in
  parallel. Classified the request as read-only at this stage.
- Sanitized terminal, CLI, and tool actions: read `ai-logs/README.md`, member
  guide, `docs/README.md`, `docs/team/now/README.md`; three parallel read-only
  exploration passes over `backend/`, `frontend/`, and the deploy surfaces.
- Command and tool exit status: success.
- Outcome or important output summary: Blocker-level findings recorded —
  production compose loading the developer `.env` with a dev database password
  and provider keys; unauthenticated agent endpoints with no rate or payload
  limit; a silent Postgres-to-Excel fallback; `print`-only diagnostics; a
  hardcoded frontend comparison table driven by a keyword heuristic; E02
  persona strings addressing the customer as "mình"/"bạn"; no chat request
  timeout; frontend CI triggering on a branch that does not exist; a stale
  root compose template with no backend or database; and an external catalog
  volume that aborts a first deploy.
- Files affected or inspected: audit only, no repository writes.
- Validation performed: none at this stage.
- Validation result: not applicable.
- Redactions or logging limitations: credential values were never read or
  recorded; only variable names and file locations were noted.

### Entry 2 — 2026-07-18T21:00:00Z

- Human request summary: Implement the plan.
- AI response or decision summary: Stopped before implementation because
  `docs/team/now/README.md` mapped no tracker to `luu-tien-duy`, and because
  US-124 and ADR-0017 both named a root compose file that is a stale
  website-cloner template. Presented both to the human. Duy assigned tracker
  alias DUY and chose the production compose file as the deploy target, plus
  three scope decisions: keep the mock flows as labelled demos, rate limit
  rather than authenticate the agent endpoints, and render the comparison
  table from backend structured data.
- Sanitized terminal, CLI, and tool actions: `harness-cli query matrix`;
  read US-124 packet and tracker files.
- Command and tool exit status: success.
- Outcome or important output summary: Read gate cleared by explicit human
  assignment; US-125 registered as a high-risk story under E06.
- Files affected or inspected: `docs/team/now/DUY-NOW.md` (created),
  `docs/team/now/README.md`, US-125 story packet (created).
- Validation performed: baseline suites before any edit.
- Validation result: backend 410 passed / 17 skipped; frontend `npm run check`
  exit 0.
- Redactions or logging limitations: none.

### Entry 3 — 2026-07-18T22:00:00Z

- Human request summary: (continuation) complete the implementation.
- AI response or decision summary: Implemented P0 to P4. Split production
  configuration into `.env.production` with a tracked example and aligned
  `.env.example` with the contract the code actually reads. Added a payload
  cap, a per-client rate limit, and an LRU session ceiling to the agent
  endpoints, with nginx `limit_req` at the edge. Replaced `print` with the
  logging module behind a new `configure_logging` entrypoint, made the
  Postgres probe log its cause and fail fast under `REQUIRE_POSTGRES`, and
  gave the LLM client one retry with logging on transient provider errors.
  Added an additive `comparison` block to the agent response built from the
  same dimension loop the reply text uses, and rewrote the frontend table as a
  presentational component driven by it. Corrected E02 persona strings, added
  an abort-based request timeout with retry, surfaced previously swallowed
  replay and feedback errors, and labelled the demo flows. Fixed the CI branch
  trigger, deleted the stale root compose file and the empty root lockfile,
  and added a deploy preflight script.
- Sanitized terminal, CLI, and tool actions: `bootstrap-harness.sh`;
  `harness-cli intake/story add/story update/story verify/trace/intervention`;
  `pytest`; `npm run check`; `docker compose config`; nginx config parse;
  `deploy-preflight.sh`; a live E02 run against a backend started on a spare
  port pointed at the already-running catalog database.
- Command and tool exit status: success; one intermediate contract-test
  failure, fixed.
- Outcome or important output summary: Backend 434 passed / 17 skipped against
  a 410 baseline, no regression. Frontend lint, typecheck, and build clean.
  Live run confirmed the comparison payload is dimension-driven for the máy
  lạnh category, that every winner in the table is stated in the reply text on
  both the plain and streaming endpoints, that placeholder specs are excluded,
  and that oversized and excessive requests return 413 and 429.
- Files affected or inspected: see Files Touched.
- Validation performed: full suites before and after; targeted new unit tests;
  live endpoint exercise; config parsing.
- Validation result: all pass; harness story verification pass.
- Redactions or logging limitations: no credential values were read, printed,
  or written. The temporary `.env.production` used to validate the compose
  file was created from the tracked example and deleted immediately.

## Files Touched

- Created: `.env.production.example`; `backend/app/logging_config.py`;
  `backend/tests/unit/agent/test_production_hardening.py`;
  `scripts/deploy-preflight.sh`; `frontend/src/components/DemoNotice.tsx`;
  `docs/team/now/DUY-NOW.md`; the US-125 story packet under
  `docs/stories/epics/E06-delivery-automation/`.
- Changed: `.env.example`; `docker-compose.production.yml`;
  `infra/nginx/default.conf`; `.github/workflows/ci.yml`; `README.md`;
  `backend/app/agent/{api,graph}.py`; `backend/app/agent/tools/compare.py`;
  `backend/app/agent/catalog/pg_adapter.py`;
  `backend/app/agent/llm/client.py`; `backend/app/api/main.py`;
  `backend/app/db/migrate.py`; `backend/app/ingestion/run.py`;
  `backend/tests/contract/test_agent_api_contract.py`;
  `frontend/src/components/{ChatbotAssistant,LoginScreen,CheckoutScreen,AccountOrdersScreen}.tsx`;
  `frontend/src/components/chat/ChatComparisonResult.tsx`;
  `frontend/package.json`; `docs/team/now/README.md`;
  `docs/decisions/0017-github-actions-ubuntu-deployment.md`; the US-124
  overview and design.
- Deleted: `docker-compose.yml` (stale website-cloner template with no backend
  or database); `package-lock.json` (empty stub at a path with no
  `package.json`, which also caused a duplicate-lockfile build warning).
- Materially inspected: `backend/app/agent/catalog/dimensions.py`;
  `backend/app/config/db_settings.py`; `docs/FEATURE_INTAKE.md`;
  `docs/CONTEXT_RULES.md`; the US-124 story packet.

## Validation

- Checks performed: full backend suite before and after; frontend lint,
  typecheck, and production build before and after; new unit tests for the
  limits, the comparison projection, the fail-fast flag, and the LLM retry;
  production compose config parse; nginx config parse; deploy preflight run;
  live E02 exercise of `/api/v1/agent/respond` and `/respond/stream`; harness
  story verification.
- Results: backend 410 → 434 passed with 17 skipped and no regression;
  frontend build clean; compose config valid; nginx parses (the only error off
  the compose network is service-name DNS, which is expected); preflight
  correctly exits 1 without `.env.production`; live comparison payload matches
  the reply text on both endpoints; 413 and 429 returned as designed; harness
  verification pass.

## Errors and Blockers

- Errors: `test_response_carries_the_fields_the_frontend_reads` asserted the
  exact response key set and failed on the additive `comparison` field. The
  assertion was updated and a case added proving the field is null off a
  comparison turn.
- Blockers: the mandatory read gate blocked implementation because no tracker
  mapped to this identity, and because US-124 and ADR-0017 named a compose
  file that turned out to be a stale template. Both were resolved by explicit
  human direction before any product code changed.
- Disposition: all resolved. One item remains open: the clean-host deploy
  drill was not run, because the host already runs the stack and tearing it
  down was not warranted.

## Final Outcome

- Status: complete for the agreed scope; not yet merged or deployed.
- Outcome summary: The deploy branch no longer ships production credentials
  from a developer file, the public agent endpoints are bounded, failures are
  observable rather than silent, the chat comparison table is generated from
  the agent's own evidence, the E02 persona is correct in the chat UI, the
  mock flows are labelled, and the deploy path and CI trigger point at real
  targets. US-125 is registered with proof, trace, and verification recorded.
- Unresolved work: rotating the exposed provider keys and setting a strong
  database password are human actions outside the repository and must happen
  before any real deploy. The clean-host deploy drill is still owed. Durable
  agent session storage (US-206), Langfuse feedback (US-207), and real NeMo
  guardrails remain deferred, as do real authentication, order placement, and
  order history.
- Suggested next actions: rotate credentials and create `.env.production` on
  the deploy host; run the preflight and the first-deploy drill; coordinate
  with Thành before merging, since US-207 also touches
  `backend/app/agent/api.py`; decide separately whether the backend's
  customer-reference "mình" register should become "anh/chị" repository-wide.

## Redaction Summary

- Redactions applied: credential values were never read, echoed, or written.
  Only variable names and file locations appear in this log and in the story
  packet. The preflight script checks credentials for emptiness without
  printing their values.
- Logging limitations: the live E02 run used a locally started backend rather
  than the deployed container, so it proves the code paths rather than the
  built image.
- Sensitive values were not intentionally recorded: confirmed.
