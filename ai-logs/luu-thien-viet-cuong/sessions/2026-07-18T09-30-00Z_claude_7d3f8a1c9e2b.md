# AI Coding Session Log

## Session Metadata

- `member`: Lưu Thiện Việt Cường
- `member_slug`: luu-thien-viet-cuong
- `ai_client`: Claude Code
- `interface`: IDE (VS Code extension)
- `session_id`: 7d3f8a1c9e2b
- `started_at_utc`: 2026-07-18T09-30-00Z (approximate; multi-compaction session)
- `task_summary`: Live-test fix round from Cường's manual transcript: guardrail rebalance (refuse only deliberate abuse; friendly smalltalk with sales pivot), four new intents with grounded flows (catalog_overview, price_range_query, promotion_inquiry, smalltalk), natural policy answers (display names, no filenames, literal frame only on request, relevance floor), hidden internal skip-role line, bundled 2-3-question cold-start opener, +8% soft budget margin, suggestion/comparison reasoning, NDJSON streaming endpoint, frontend copy/edit/status/streaming UX.
- `repository`: AI-Hackathon-2026
- `branch`: main

## Objective

Cường live-tested the deployed agent and reported twelve defect groups with
transcripts, with two standing instructions: always enter Plan mode before any
fix, and always update every affected document. New guard principle set by
Cường: refusal only on deliberate exploitation/injection/repeated off-topic;
basic conversation must be handled warmly like a real Điện Máy Xanh
salesperson with a sales pivot; catalog discovery runs rule-based.

## Interaction Log

### Entry 1 — 2026-07-18 (root-cause analysis and approved plan)

- Human request summary: Fix guardrail overfire ("bán cái gì", "dạo này nóng
  quá", "tivi có hàng…", thanks all refused) and underfire; hide the internal
  "(Em chưa đủ dữ liệu để chấm mục…)" line; price-range question misrouted to
  policy showing a filename and "quy định nguyên văn" framing; promotion
  question misrouted to policy; "tư vấn gia dụng có những loại hàng nào"
  dumped products; cold-start should bundle 2-3 questions; budget 20-30tr must
  accept ~31tr; suggestions/comparison must reason from intent + context;
  frontend needs copy/edit buttons, natural progress status, and streaming.
- AI response or decision summary: Traced every symptom to code:
  `degraded_fail_closed` blocked all non-shopping text (overfire); the intent
  set lacked price-range/promotion/catalog/smalltalk so keyword fallback
  landed on `policy_question`; the policy renderer printed the corpus
  filename and a hard verbatim frame; the skip-role disclosure rendered
  user-facing; budget filtering was hard; cold-start asked one question per
  turn. Wrote the plan (Parts A–F), Cường approved in Plan mode.
- Sanitized terminal, CLI, and tool actions: Reads across graph/understand/
  respond/policies/tools; plan file write.
- Command and tool exit status: Plan approved.
- Outcome or important output summary: Full root-cause map; deterministic
  router chosen over a ReAct loop (testability), recorded in the architecture
  doc.
- Files affected or inspected: `backend/app/agent/**`, frontend chatbot
  component, golden eval set.
- Validation performed: Each symptom reproduced against code paths before
  planning.
- Validation result: All twelve symptoms explained by five root causes.
- Redactions or logging limitations: None.

### Entry 2 — 2026-07-18 (backend implementation, Parts A–D)

- Human request summary: Execute the approved plan.
- AI response or decision summary: Implemented: guard rebalance in `run_turn`
  (`degraded_fail_closed` no longer blocks agent input; injection/credential/
  unsafe-exec/exploit/word-limit still refuse); four new `AgentIntent` values
  with deterministic fallback markers and LLM-extractor prompt rules; four
  new grounded flows (`_smalltalk_flow` with thanks/weather/agreement
  handling and mid-flow agreement continuing the product flow;
  `_catalog_overview_flow` menu/per-category overview; `_price_range_flow`
  from aggregate price min/max; `_promotion_flow` with graceful degradation);
  policy naturalness (`DISPLAY_NAMES`/`display_source`, no filenames,
  literal-quote frame only when the customer asks, `min_score` relevance
  floor); hid the skip-role line; bundled cold-start opener
  (`opening_questions`/`render_opening`, ①②③); `budget_slack=0.08` soft
  margin; suggestion reasoning (need summary opener, per-product fit reasons,
  missing-info hint, `format_trieu` so need amounts pass grounding);
  comparison reasoning (delta savings, pricier perks, context lean); NDJSON
  `POST /api/v1/agent/respond/stream`.
- Sanitized terminal, CLI, and tool actions: Code edits across 11 backend
  files; pytest iterations.
- Command and tool exit status: Agent suite green after fixes.
- Outcome or important output summary: All backend symptoms fixed
  deterministically; no new LLM dependency for the fixes.
- Files affected or inspected: `contracts.py`, `graph.py`, `respond.py`,
  `api.py`, `understand.py`, `coldstart.py`, `policies/answer.py`,
  `policies/corpus.py`, `tools/search.py`, `llm/client.py`,
  `prompts/CLARIFY.md`.
- Validation performed: Full backend suite; four initially failing tests
  updated to the new natural formats (transport assertion, degradation
  wording, verbatim-body check, golden AGENT-G-003).
- Validation result: 321 passed, 13 skipped at round close (skips are Duy's
  DB-docker tests).
- Redactions or logging limitations: None.

### Entry 3 — 2026-07-18 (golden cases, frontend UX, docs)

- Human request summary: Same round continuation ("tiếp tục"; "vừa pull về,
  kiểm tra lại phần nối UI").
- AI response or decision summary: Added golden cases AGENT-G-013…021 from
  the failure transcript (catalog menu, weather pivot, thanks, washer price
  range, promotion inquiry, gia-dụng overview, tivi not refused, policy
  no-filename, bundled opener) plus `not_contains`/`verbatim_body`
  expectation support in the eval runner. Frontend `ChatbotAssistant.tsx`:
  streaming reader over `/respond/stream` (NDJSON, incremental render),
  `statusForQuery` natural progress line replacing "...", copy button on
  assistant bubbles, edit button on user bubbles (reloads composer),
  `pendingStatus` state. Docs updated: architecture §2 flow, §5 bundled
  opener, §6 natural policy, §8 guard policy, §9 13 intents + ReAct
  deferral note, §10 streaming endpoint; E02 README round history; CLARIFY
  prompt multi-question opener.
- Sanitized terminal, CLI, and tool actions: TSX edits; doc edits; golden
  JSONL edits.
- Command and tool exit status: Frontend compile verified via dev container
  (HTTP 200 on `/`, ChatbotAssistant imported by the root layout).
- Outcome or important output summary: Full UX round delivered; streaming is
  presentation-level (deterministic pipeline produces full grounded text,
  then chunk-streams; token-level arrives when the sell step runs on a
  streaming LLM) — recorded in the architecture doc.
- Files affected or inspected: `frontend/src/components/ChatbotAssistant.tsx`,
  `data/agent-eval/agent-golden-cases.jsonl`,
  `backend/tests/eval/test_agent_golden.py`, architecture doc, E02 README,
  `CLARIFY.md`.
- Validation performed: Golden suite; container compile check.
- Validation result: Green.
- Redactions or logging limitations: None.

### Entry 4 — 2026-07-18 (verification, harness, services)

- Human request summary: Same round closeout.
- AI response or decision summary: Full-suite rerun found one failure that is
  NOT from this round: `backend/tests/contract/test_m1_contracts.py::
  test_advisor_state_freezes_required_workflow_fields` fails on Python 3.11
  because `backend/app/contracts/schemas.py` (pulled commit 8ce3b51, another
  lane) uses `typing.TypedDict` with pydantic, which requires
  `typing_extensions.TypedDict` on <3.12. Not touched (file ownership);
  reported to the owner via this log and the walkthrough — one-line fix is
  importing `TypedDict` from `typing_extensions`. The story verify commands
  run under `uv --python 3.12`, where everything passes. Harness: intake #15,
  US-204/205/206 changed→re-completed with fresh proof (19, 13, and 104
  tests respectively), trace #20 at detailed 3/3 (traces #18/#19 are
  superseded partial recordings), audit entropy 0/100, propose returned
  none. Services restored for live testing: Docker Desktop + frontend dev
  container (3010, compile clean), agent API uvicorn on 8010 with CORS for
  3010.
- Sanitized terminal, CLI, and tool actions: pytest; harness-cli intake/
  story/trace/audit/propose; docker start/logs; uvicorn background start.
- Command and tool exit status: Agent scope fully green; pre-existing M1
  failure flagged, not fixed.
- Outcome or important output summary: Round landed with proof; environment
  ready for Cường's re-test.
- Files affected or inspected: `harness.db` via CLI; this log.
- Validation performed: Story completion proofs; audit; propose; health/
  compile checks.
- Validation result: Entropy 0/100; no proposals.
- Redactions or logging limitations: Trace durations/tokens are labeled
  coarse estimates.

### Entry 5 — 2026-07-18 (live-test round 2: report and approved plan)

- Human request summary: Second manual live test found: inconsistent
  self-address ("Bạn/Mình" vs "em" — must ALWAYS be "em"); missing
  like/dislike buttons; edit button behaves like copy-paste; "có laptop
  không?" gets a menu dump instead of an honest "không"; suggestions lack
  benefit/drawback debate; "mục đích sử dụng" asked without examples;
  answered budget re-asked; "tầm 20-30 trịu" not captured; "máy đắt nhất đi
  em" → menu dump; "mục đích sử dụng tủ lạnh á?" → nonsense data-privacy
  apology with a broken chunk ("b."). New instruction: use gpt-4o-mini as
  the main reasoning core with the newly added key; enrich tools; write
  golden cases (basic + hard); always live-test before approving; always
  Plan mode first.
- AI response or decision summary: Traced all roots read-only, entered Plan
  mode, Cường approved. Root causes: budget regex missed typo units;
  `apply_turn` reset the asked-list on every material new_search (clobbering
  an active cycle); stateless fallback dumped menus mid-flow; no
  unavailable-product branch; extractor misroutes + off-topic privacy match
  (generic folded words scored the heading) + validation-failure falling
  into the violation apology; no debate line; frontend strings/buttons.
  Architecture verdict recorded: no ReAct needed yet — LLM-first extractor
  with full context + state-aware fallback is the fix.
- Sanitized terminal, CLI, and tool actions: Read-only trace; plan file.
- Command and tool exit status: Plan approved.
- Outcome or important output summary: 8 defect groups mapped to 5 root causes.
- Files affected or inspected: understand/memory/graph/respond/corpus/client,
  frontend component, `.env` key NAMES only (values never read).
- Validation performed: each symptom reproduced against code paths.
- Validation result: consistent.
- Redactions or logging limitations: `.env` values never read into context.

### Entry 6 — 2026-07-18 (round-2 implementation and verification)

- Human request summary: Execute the approved round-2 plan.
- AI response or decision summary: Implemented: intent 14
  (`question_clarification`) with deterministic echo pre-route (explains the
  bot's own question with per-category examples; echo never captured as an
  answer); typo-tolerant money parsing (trịu/trieu/tr/củ, ranges); state-aware
  fallback (active category → mid-flow continuation, never menu; "đắt
  nhất/xịn nhất" → new `most_expensive` suggestion role); honest
  unavailable-product replies with nearest in-catalog alternatives
  (laptop/tivi/điện thoại/…); asked-list reset now only reopens a SPENT
  clarification cycle; gaming follow-ups trigger on "chơi game" (trigger
  "gam" — found via a failing test that "game" is not a substring of
  "gaming"); question examples in scenario data; missing-info hint skips
  answered fields and carries a purpose example; "So nhanh" debate line
  (price delta + counter-perk, validator-grounded); policy hardening
  (mục/đích/sử/dụng stopwords, orphan-enum tail trim, validation failure →
  graceful no-info instead of the violation apology). Reasoning core: probing
  with key NAMES/lengths only revealed `OPENAI_API_KEY` is present but EMPTY —
  Cường's new key is the OpenRouter key — so the core runs
  `openai/gpt-4o-mini` via OpenRouter (direct OpenAI auto-preferred if the
  key is ever filled; `AGENT_MAIN_LLM_MODEL` overrides; M1's DEFAULT_MODEL
  no longer leaks into the agent — the deepseek route 429s persistently).
  Extractor JSON now coerces near-miss types (deepseek returned
  `"category_code": 38` as int — the cause of the flaky
  `understanding_degraded` turns) and the prompt forbids inferring
  purpose/priorities the customer did not state; extractor context enriched
  (need, asked keys, pending question TEXT, last 3 exchanges). Capture no
  longer suppressed by a redundant category signal. Frontend: consistent
  "em" self-address across all strings, like/dislike buttons POSTing to new
  `POST /api/v1/agent/feedback` (in-memory + log), replay-based edit (cuts
  the conversation, replays prior user turns on a fresh session), copy kept.
- Sanitized terminal, CLI, and tool actions: pytest (unit/golden); extractor
  probes with masked config; live replay script against 8010; harness
  intake #16, US-204/205/206 changed→re-completed; docker/uvicorn restarts.
- Command and tool exit status: Agent unit + golden: **151 passed**; golden
  set now 36 cases (AGENT-G-022…036 added; G-019 updated to the honest
  format). Live replay of both failing transcripts + "có laptop không?"
  reproduced every fix: budget "20-30 trịu" captured with in-range
  suggestions and the gaming follow-up (no budget re-ask), "máy đắt nhất đi
  em" → Cao cấp nhất 30.99tr with soft-margin note, "mục đích sử dụng tủ
  lạnh á?" → example-backed clarification, laptop → honest alternatives,
  debate line present, extractor turns clean on gpt-4o-mini.
- Outcome or important output summary: All 8 defect groups fixed and
  verified live per Cường's "always live-test before approving".
- Files affected or inspected: `contracts.py`, `understand.py`, `memory.py`,
  `coldstart.py`, `scenario_data.py`, `suggest.py`, `respond.py`,
  `corpus.py`, `graph.py`, `llm/client.py`, `api.py`,
  `ChatbotAssistant.tsx`, golden JSONL, `test_livetest2_round.py`,
  `test_refinements.py`, architecture doc, E02 README, AGENTS.md learnings.
- Validation performed: RED test caught the gaming-trigger substring bug;
  full agent suite; live transcript replay; extractor probes.
- Validation result: Green + live-verified.
- Redactions or logging limitations: API keys never printed — presence and
  length checks only.

## Files Touched

- Created: this log
- Changed: `backend/app/agent/contracts.py`, `graph.py`, `respond.py`,
  `api.py`, `conversation/understand.py`, `conversation/coldstart.py`,
  `policies/answer.py`, `policies/corpus.py`, `tools/search.py`,
  `llm/client.py`, `prompts/CLARIFY.md`,
  `frontend/src/components/ChatbotAssistant.tsx`,
  `data/agent-eval/agent-golden-cases.jsonl`,
  `backend/tests/eval/test_agent_golden.py`,
  `backend/tests/integration/agent/test_smoke.py`,
  `backend/tests/unit/agent/policies/test_policy_engine.py`,
  `backend/tests/unit/agent/test_platform_round.py`,
  `docs/product/architecture/multi-category-agent.md`,
  `docs/stories/epics/E02-multi-category-agent/README.md`; `harness.db` via CLI
- Deleted: None
- Materially inspected: sample-chat tone references, aggregate tool, frontend
  layout wiring

## Validation

- Checks performed: Full backend suite; golden cases incl. nine new
  transcript-derived cases; story completion proofs under uv Python 3.12;
  frontend compile via dev container; harness audit + propose.
- Results: Agent scope green (320 passed + 13 skipped on Python 3.11 with one
  pre-existing M1 contract failure from another lane; 104/104 agent
  unit+integration under the US-206 proof); entropy 0/100; no proposals.

## Errors and Blockers

- Errors (all resolved in-scope): four tests asserting the old robotic
  formats updated alongside the format change; one frontend edit anchor
  mismatch retried with the correct surrounding lines; harness trace
  vocabulary discovered by trial (outcome enum `completed`, detailed-tier
  fields), superseded traces #18/#19 noted.
- Blockers: None for this round. Team-side: pre-existing M1 contract test
  failure on Python 3.11 from commit 8ce3b51 (`typing.TypedDict` in
  `backend/app/contracts/schemas.py`) — owner should switch the import to
  `typing_extensions.TypedDict`.
- Disposition: Flagged to the owning lane; no unilateral edit.

## Final Outcome

- Status: Completed (two live-test rounds in one session)
- Outcome summary: Every defect from Cường's live-test transcript is fixed
  and regression-covered: the guard no longer refuses friendly/basic
  messages, price-range/promotion/catalog questions route to grounded
  catalog flows instead of policy, policy answers read naturally with
  human-readable source names, the internal skip-role line is hidden, the
  cold-start opener bundles 2-3 questions, budgets have an 8% soft margin,
  suggestions and comparisons explain their reasoning from the captured
  need, and the frontend has copy/edit buttons, a natural progress status,
  and chunk-streamed rendering over the new NDJSON endpoint. Stories
  US-204/205/206 re-completed with fresh proof; entropy 0/100.
  Round 2 additionally delivered the gpt-4o-mini reasoning core (OpenRouter
  route, type-coerced extraction, no-fabrication prompt), state-aware
  routing (question_clarification, most_expensive, honest unavailable
  products, no menu dumps mid-flow), the asked-list reset fix, question
  examples + suggestion debate, policy chunk hardening, and the frontend
  feedback/replay-edit/"em" pass — all verified by replaying the exact
  failed transcripts against the live API.
- Unresolved work: Token-level streaming (waits on an LLM-driven sell step);
  pre-existing M1 contract failure owned by another lane; Langfuse judge and
  pgvector policy retrieval remain deferred; `OPENAI_API_KEY` in `.env` is
  an empty stub — filling it switches the core to direct OpenAI
  automatically.
- Suggested next actions: Cường re-runs the failing transcripts live on
  3010/8010; owning lane patches the `typing.TypedDict` import; consider
  LLM-streamed sell for true token streaming.

## Redaction Summary

- Redactions applied: None
- Logging limitations: Timestamps approximate; trace durations/tokens are
  coarse estimates.
- Sensitive values were not intentionally recorded: Confirmed. `.env` values
  were never read into context or logs.
