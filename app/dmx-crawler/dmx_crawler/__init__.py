"""A small, auditable Điện Máy XANH crawler.

The package deliberately uses the Python standard library for its core path so
that a sample crawl and the parser tests can run in a clean environment.  A
PostgreSQL migration and an optional psycopg extra are supplied for production.
"""

__version__ = "0.1.0"
